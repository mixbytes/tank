# Binding names are not predefined, you can introduce any bindings you feel suitable
polkadot:

    # Ansible part of the binding
    ansible:
        # Git link to fetch the binding
        src: https://github.com/mixbytes/tank.ansible-polkadot

        # Optionally - branch/tag/commit to check out
        version: master

haya:
    ansible:
        src: https://github.com/mixbytes/tank.ansible-haya
